# Copy to .env and fill in. The CLI loads this automatically (see cli.load_dotenv).
# Only the key for your chosen --model backbone is needed.
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# AUTOML_MODEL=gpt-4o   # default model if --model is not passed, e.g. claude-opus-4-8
